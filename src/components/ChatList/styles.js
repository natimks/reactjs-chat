import styled from 'styled-components';

export const Container = styled.div`
  height: auto;
  width: 30%;
  position: relative;
  z-index: 1;
  top: 0;
  overflow-x: hidden;
  margin-top: 0.5rem;
`;
